import 'package:flutter/material.dart';

class DetailsRoutes extends StatelessWidget {
  const DetailsRoutes({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('UWU');
  }
}
